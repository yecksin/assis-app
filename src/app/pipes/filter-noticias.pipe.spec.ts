import { FilterNoticiasPipe } from './filter-noticias.pipe';

describe('FilterNoticiasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNoticiasPipe();
    expect(pipe).toBeTruthy();
  });
});
